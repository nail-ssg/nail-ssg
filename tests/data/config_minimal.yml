core:
  src: tests/data/src1
  dist: tests/data/site
  currentNamespace: default
  modules:
    pages: true
  main: main
scan:
  order:
  - pages
  - static
  main: 
  pages:
    folder: pages
  types:
  - type: page
    extractData: true
    rules:
    - fileMask = *.html
    - regExp = \.page\.
